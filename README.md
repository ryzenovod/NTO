# NTO — набор алгоритмических задач

Современное хранилище решений и черновиков для подготовки к техническим заданиям. В репозитории собраны небольшие утилиты на C# и Python, тестовые данные и вспомогательные материалы, которые можно быстро запускать локально.

## 📂 Структура
- **2024/** — консольные программы на C# (задачи с входом через `input.txt` или встроенные данные).
- **2nd_stage/** — Python-решения для второго этапа (например, расчёт видимости спутников). Внутри — как код, так и поддиректории с ресурсами.
- **engineer_stage_entry/** — компактные скрипты на Python для инженерного этапа.
- **A_tests/** — набор входных/выходных файлов (`*.in`/`*.out`) для локальной проверки задач.
- **Unity.gitignore** — шаблон `.gitignore` для Unity-проектов, если понадобится собирать прототипы.

## 🚀 Быстрый старт
1. Установите зависимости:
   - **C#**: .NET SDK 6+ (`dotnet --version` для проверки).
   - **Python**: версия 3.9+ (рекомендуется создание `venv`).
2. Склонируйте репозиторий и перейдите в его корень:
   ```bash
   git clone <repo-url>
   cd NTO
   ```
3. Запустите нужное решение:
   - C#: 
     ```bash
     cd 2024
     dotnet new console -n Scratch --force > /dev/null 2>&1 && rm -rf Scratch
     # Пример компиляции Program1
     csc Program1.cs
     ./Program1
     ```
   - Python:
     ```bash
     cd 2nd_stage
     python task1_vasya.py < input.txt
     ```

## 🧪 Локальная проверка
- Сравнивайте вывод с эталонными файлами из `A_tests/` (напр., `diff <(./Program1 < A_tests/01.in) A_tests/01.out`).
- Поддерживайте чистоту линтером: `flake8`/`ruff` для Python и `dotnet format` для C# (по желанию).

## 🤝 Как вносить изменения
1. Создайте ветку, связав её с задачей или тикетом.
2. Соберите и проверьте решение локально.
3. Оформите понятный коммит: описывайте **что** и **зачем** изменено.
4. Откройте Pull Request с кратким описанием проблемы и итогов.

## 📌 Полезно помнить
- Скрипты предполагают запуск из соответствующих папок, где лежат входные файлы.
- Для воспроизводимых результатов фиксируйте версии зависимостей в `requirements.txt` или `global.json`, если начнёте расширять проект.

Успехов в решении задач!
